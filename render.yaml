services:
  - type: web
    name: pokemon-whatsapp-bot
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: bot-data
      mountPath: /data
      sizeGB: 1